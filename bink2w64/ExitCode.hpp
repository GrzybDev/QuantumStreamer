// ExitCode.hpp : Defines the ExitCode enumeration, which represents the possible exit codes of the application.
#pragma once

enum ExitCode
{
	OK = 0,
	DLL_LOAD_FAILURE = 1,
	PROXY_STARTUP_FAILURE = 2,
};
